# jest-expo@57.0.x peer-depends on @react-native/jest-preset@^0.86.3, but the pinned
# react-native@0.86.2 resolves it to exactly 0.86.2 (a patch-level packaging skew in
# Expo SDK 57). Loosen peer resolution so `npm install` / `npx expo install` succeed.
legacy-peer-deps=true
